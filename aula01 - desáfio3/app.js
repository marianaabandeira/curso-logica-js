// 1. Contador de 1 até 10

contador = 1;

while (contador <=10) {
    console.log(contador);
    contador++;
}

// 2. Contador de 10 até 1

contador = 10;
    while (contador >= 0) {
        console.log(contador);
        contador--;
    }

// 3. Contagem regressiva a partir de um número escolhido

let numero = Number(prompt("Digite um número para a contagem regressiva:"));

while (numero >= 0) {
    console.log(numero);
    numero--;   
}

// 4. Contagem progressiva até um número escolhido

let numero = Number(prompt("Digite um número para a contagem progressiva:"));

while (numero <= numero) {
    console.log(contador);
    numero++;   
}

